# Tooling

- Node.js 18.x and npm (use `nvm` to match project defaults).
- Expo CLI via `npx expo` (global install optional).
- Watchman (recommended on macOS for stable file watching).
- Xcode with the iOS Simulator for `expo start --localhost` + `i`.
- Android Studio and an emulator image for `expo run:android` (optional).
